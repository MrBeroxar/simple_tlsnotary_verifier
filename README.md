# Simple TLSNotary Verifier in Rust

This is a minimal TLSNotary proof verifier implemented in Rust.

An example notary proof is provided in /data/

To build and run the verifier:
```bash
cargo build --release
cargo run --release